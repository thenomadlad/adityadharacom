---
interface Props {
  date: Date;
}

const { date } = Astro.props;

const dateVal = new Date(date);
---

<time datetime={dateVal.toISOString()}>
  {
    dateVal.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
